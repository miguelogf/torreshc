---
export interface Props {
  variant?: "primary" | "outline" | "ghost";
  type?: "button" | "submit" | "reset";
  href?: string;
  newTab?: boolean;
  fullWidth?: boolean;
  class?: string;
  [key: string]: any;
}

const {
  variant = "primary",
  type = "button",
  href,
  newTab = false,
  fullWidth = false,
  class: className = "",
  ...rest
} = Astro.props;

const base =
  "inline-flex items-center justify-center gap-2 px-6 py-2 text-sm font-medium rounded-full cursor-pointer no-underline transition-all duration-200 hover:opacity-90 hover:scale-[1.03] active:scale-[0.96] focus-visible:outline focus-visible:outline-1 focus-visible:outline-primary focus-visible:outline-offset-2";

const variants = {
  primary: "bg-primary border border-primary text-white",
  outline: "bg-dark/5 border border-dark/50 text-dark",
  ghost:
    "bg-transparent border-transparent text-dark opacity-50 px-2 hover:bg-dark/5",
};

const classes = [base, variants[variant], fullWidth ? "w-full" : ""].join(" ");
---

{
  href ? (
    <a
      href={href}
      target={newTab ? "_blank" : undefined}
      rel={newTab ? "noopener noreferrer" : undefined}
      class:list={[classes, className]}
      {...rest}
    >
      <slot />
    </a>
  ) : (
    <button type={type} class:list={[classes, className]} {...rest}>
      <slot />
    </button>
  )
}
